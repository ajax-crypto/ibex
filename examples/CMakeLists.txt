# Examples are not built yet since compiler is still in development
# add_executable(example_hello
#     hello.ibex
# )

# Placeholder - would normally invoke ibexc compiler
# add_custom_command(TARGET example_hello POST_BUILD
#     COMMAND ibexc ${CMAKE_CURRENT_SOURCE_DIR}/hello.ibex -o example_hello
# )
